"""
Solving algorithm
"""
import Lazor


def solve(board):
    # From the blocks that are available, place them in random spots on the board
    
    while(positions != Filled):
    	move block
    	Refresh board

if __name__ == "__main__":
	solve()
